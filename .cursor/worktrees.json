{
  "setup-worktree": [
    "test -f \"$ROOT_WORKTREE_PATH/.env\" && cp \"$ROOT_WORKTREE_PATH/.env\" .env || true",
    "test -d api && (cd api && go mod download) || true",
    "test -d compute && (cd compute && cargo fetch) || true"
  ]
}
