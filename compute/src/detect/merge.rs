//! Слияние спанов детекторов: пересечения и ПИН-гейт.

use crate::models::Span;

/// Сливает спаны: сортирует, разрешает пересечения и применяет ПИН-гейт.
pub fn merge_spans(spans: Vec<Span>) -> Vec<Span> {
    todo!("реализация слияния спанов")
}