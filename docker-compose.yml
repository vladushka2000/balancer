version: "3.8"
services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  server:
    build: .
    environment:
      - REDIS_URL=redis://redis:6379/0
      - PII_RPS_TARGET=1500
      - PII_APP_PORT=8080
    ports:
      - "8080:8080"
    depends_on:
      - redis