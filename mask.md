# Mask — R&D качества маскирования

Как сделать forward-маску максимально близкой к эталону чекера и при этом
оставить текст осмысленным для LLM. Reverse **не** зависит от формата:
vault по `payload_id` возвращает оригинал.

Код сейчас: [`internal/compute/mask/rules.go`](internal/compute/mask/rules.go),
[`internal/compute/mask/masker.go`](internal/compute/mask/masker.go).

---

## 0. Что реально известно про эталон

**Единственная опубликованная пара** (OpenAPI в [`main.md`](main.md)):

| | |
|---|---|
| Вход | `Клиент Иванов Иван Иванович, паспорт 4509 123456` |
| Эталон | `Клиент И. И. И., паспорт 45** ****56` |

Из неё следует:

- ФИО → инициалы `X. Y. Z.` (точка, пробел).
- Цифры паспорта `4509 123456` → `45** ****56` (2 + `**`, пробел, `****` + 2).
- Слова `Клиент` и `паспорт` **не** маскируются.
- Длина спана **может** сильно измениться (ФИО короче). «Стабильная длина любой ценой» не следует из примера.

Формула «нормированное span-based расстояние Левенштейна [0..1]» в Приложении B
**не раскрыта**. Вероятнее всего либо Lev по выровненным спанам, либо
`Lev(full) / max(len)` по всей строке. Presidio считает IoU спанов, не Lev
масок — это соседняя метрика, не формула жюри.

Рабочая цель: минимизировать правки относительно стиля примера.
Шаблоны ниже для остальных типов — **экстраполяция** (PCI / `hack.md`), не
официальный словарь эталона. Проверять фикстурами, как только появится
хотя бы несколько пар.

---

## 1. Политика, которая двигает score сильнее всего

**Маскировать только VALUE, маркеры оставлять.**

`паспорт`, `серия`, `номер`, `CVV`, `пин-код`, `код подразделения`, `ИНН`,
`адрес` в эталоне живут как обычный текст. Если спан детектора широкий
(`CVV 123`, `серия 4509 номер 123456`) и маскер затирает весь спан звёздами,
каждый символ маркера — штраф Левенштейна.

```
replacement = rewrite(spanText, maskValue(extractValue(spanText)))
```

Маркеры и разделители вне цифр/ФИО не трогать.

---

## 2. Баги текущего маскера (must-fix)

| # | Где | Что происходит | Почему бьёт качество | Как чинить |
|---|---|---|---|---|
| 1 | `MaskPassport` | Если `(\d{4})\s*(\d{6})` не матчится — спан возвращается **как есть** | Сырой номер vs `45** ****56` ≈ максимум дистанции | Всегда найти `(\d{4})\D*(\d{6})`; иначе зазвездить все цифры в спане |
| 2 | `MaskCVV` / `MaskPIN` | `Repeat("*", len(span))` | `CVV 123` → `*******`, слово `CVV` исчезает | Заменить только `\d{3,4}` внутри спана |
| 3 | `MaskDepartmentCode` | Всегда литерал `***-***` | Широкий спан «код подразделения 770-001» схлопывается | `ReplaceAll` только `\d{3}-\d{3}` |
| 4 | `MaskDriverLicense` | Каждый digit-run отдельно | `77 АА 123456`: `77` (len 2) → `**`, `123456` → `12** ****56` | Одна маска на номер целиком, буквы серии оставить или звёздить отдельно |
| 5 | `MaskEmail` | `email[0]` — байт | UTF-8 в local-part рвётся | Первая руна local + `*` на остальные руны |
| 6 | `MaskCard` | Только 4×4 | 13–19 цифр (в т.ч. 18/19) остаются сырыми | Снять разделители, Luhn уже в детекторе; BIN4 + mid `*` + last4 |
| 7 | `MaskDate` | Только `dd.mm.yyyy` | «12 января 1990», `1990-01-12`, мм.дд.гггг не маскируются | День+месяц скрыть, год оставить; текстовый месяц — по рунам |
| 8 | `MaskINN` | Вставляет пробелы: `77** ****** 93` | Длина и раскладка ≠ исходным 10/12 цифрам | `s[:2] + stars(n-4) + s[n-2:]` без новых пробелов, пока фикстура не докажет обратное |
| 9 | `MaskAddress` | Город целиком, нет кв/индекса/страны; комментарий ≠ код | ТЗ просит компоненты отдельно | Правила на страну, индекс, город, улицу, дом, квартиру |
| 10 | Phone/card/passport | Regex miss → raw | Тихий пропуск ПД | Fallback: звёзды на digit-run, оставить первую/последнюю группу |

`Masker.Apply` идёт слева направо по исходным индексам. При непересекающихся
спанах это эквивалентно right-to-left. Пересечения должен резать merge, не маскер.

---

## 3. Шаблоны по типам

Стиль: как в OpenAPI-примере + маркеры снаружи VALUE.

### ФИО / держатель карты

Каждое слово → первая руна (верхний регистр) + `.`, через пробел.

| До | После |
|---|---|
| `Иванов Иван Иванович` | `И. И. И.` |
| `Сидорова Анна` | `С. А.` |
| `иванов иван иванович` | `И. И. И.` |
| `IVAN IVANOV` (держатель) | `I. I.` |

Для LLM инициалы хуже частичного `И*****`, но **пример эталона — инициалы**.
Держаться примера, не «более осмысленной» маски.

### Паспорт

VALUE: из 4+6 цифр собрать `SS** ****NN`. Слова `серия` / `номер` / `паспорт` оставить.

| До | После |
|---|---|
| `паспорт 4509 123456` | `паспорт 45** ****56` |
| `серия 4509 номер 123456` | `серия 45** номер ****56` |
| `Паспорт:4509-123456` | `Паспорт:45** ****56` |

### В/У

Не резать каждый прогон цифр. Регион (2) + буквы + номер (6):
`77 АА 123456` → `77** ****56` (буквы внутри VALUE тоже скрыть, если они часть номера) либо `77 ** ****56`.
Один шаблон на весь номер, маркеры `в/у` / `водительское` не трогать.

### ИНН

Длину не менять: `7707083893` → `77******93`, `500100732259` → `50********59`.
Префикс `ИНН ` остаётся.

### Телефон

Сохранить `+7`/`8` и последние 2 цифры. Если группы не разобрались — не оставлять raw:
`+79123456789` → `+79******89`.

Канон при удачном разборе: `+7 912 345-67-89` → `+7 9** ***-**-89`.

### Email

`ivan@bank.ru` → `i***@bank.ru`. Домен целиком (смысл «это почта банка» для LLM).
`ёлка@mail.ru` → `ё***@mail.ru`.

### Карта

`4111 1111 1111 1111` → `4111 **** **** 1111`.
Слитно: `4111111111111111` → `4111********1111`.
18 цифр: первые 4 и последние 4, середина `*`.

### CVV / ПИН

| До | После |
|---|---|
| `CVV 123` | `CVV ***` |
| `пин-код 1234` | `пин-код ****` |

ПИН маскировать только если в том же payload есть карта (плюс ТЗ; гейт уже в merge).

### Даты (рождения и выдачи)

Год виден, день и месяц нет — смысл «человек 1990 года» для LLM и близость к `**.**.1990`.

| До | После |
|---|---|
| `12.01.1990` | `**.**.1990` |
| `1990-01-12` | `1990-**-**` |
| `12 января 1990` | `** ****** 1990` |

Голая дата без маркера — не спан (это детектор, не маскер).

### Адрес по компонентам

| Компонент | До | После |
|---|---|---|
| город | `Москва` | `М*****` |
| улица | `ул. Тверская` | `ул. Т*******` |
| дом | `д. 15` | `д. **` |
| квартира | `кв. 42` | `кв. **` |
| индекс | `123456` | `******` |
| страна | `Россия` | `Р*****` |

Город отделения банка не должен доходить до маскера (deny-list в detect).

### Гражданство, место рождения, орган выдачи

`MaskWord`: первая руна + `*` на остальные руны слова. Служебные `г.` / `ул.` не маскировать.

`место рождения: г. Тула` → `место рождения: г. Т***`.

### Код подразделения

`код подразделения 770-001` → `код подразделения ***-***`.

---

## 4. Что не делать на hot path

| Идея | Почему не сейчас |
|---|---|
| FPE / synthetic | Плюс ТЗ. Другой алфавит, чем partial-эталон → forward-score падает, если чекер ждёт `45** ****56` |
| Redact `[PASSPORT]` | Смысл для LLM хуже, дистанция до эталона максимальна |
| Инверсия маски на reverse | Чекер шлёт **нашу** строку; lookup точнее и не зависит от шаблона |
| Подгонка пробелов ИНН «как в hack.md» без фикстуры | Лишние символы = лишние edits |

---

## 5. Порядок внедрения

1. VALUE-only: паспорт, CVV, ПИН, код подразделения, В/У (пункты 1–4).
2. ИНН без смены длины, email по рунам, карта 13–19, даты текстом.
3. Адрес по компонентам; фикстура OpenAPI-пары должна давать **нулевую** дистанцию.
4. Selfcheck: roundtrip 100% + Lev на известных парах. Неизвестные типы — не выдавать за эталон.
5. FPE/synthetic — отдельный `mask_mode` после зелёного partial.

---

## 6. Фикстуры, без которых нельзя «доказать» 95%

Минимум, которого нет в коде как оракула:

- OpenAPI-пара (паспорт + ФИО) — ожидаемая дистанция 0.
- `серия 4509 номер 123456` — маркеры целы, цифры в стиле `45**` / `****56`.
- `CVV 123`, `пин-код 1234` — слова целы.
- `12 января 1990` при маркере «дата рождения».
- Адрес с кв. и индексом.
- Негатив: `Александр Пушкин`, `отделение банка по адресу …`, голая дата — строка не меняется.

Пока эталонного датасета нет, качество = эти фикстуры + отсутствие raw ПД при промахе regex.
